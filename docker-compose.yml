services:
  apiapp:
    build:
      context: ./api/
    networks:
     - red_docker
    ports:
      - 3000:3001

  dashboardapp:
    build:
      context: ./dashboard/
    networks:
     - red_docker
    ports:
      - 80:80

networks:
  redConexion:
    driver: bridge